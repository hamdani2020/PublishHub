terraform {
  backend "s3" {
    bucket       = "YOUR_STATE_BUCKET_NAME"
    key          = "publishhub/terraform.tfstate"
    region       = "us-east-1"
    use_lockfile = true
    encrypt      = true
  }
}
